# 복붙 프롬프트팩

커서·러버블·v0·볼트·Claude 등 **어떤 AI 툴이든** 붙여넣어 쓰세요.
내 프로젝트를 열어둔 상태에서 아래 프롬프트를 그대로 복사 → 붙여넣기 하면 됩니다.

> **아직 안 만들었으면 맨 위 "🏗️ 첫삽"부터, 다 만들었으면 "🎯 한 방에 다"부터.**

---

## 🏗️ 첫삽 — 만들기 전에 (와이어프레임 + 함정 미리 심기)

> 코드 짜기 전이라면 이것부터. 아이디어를 던지면 화면 시안과 "처음부터 조심할 것"을 같이 뽑아줍니다. 배포 직전에 고치면 재작업이지만, 여기서 미리 정하면 공짜입니다.

> 나는 이런 걸 만들려고 해: **[여기에 아이디어 한두 줄 — 무엇을, 누구를 위해]**.
> 코드 짜기 전에 아래 순서로 도와줘.
>
> 1. **와이어프레임**: 의존성 없는 **정적 HTML 한 파일**로 주요 화면(예: 홈·목록·상세·어드민)을 회색 박스 수준의 저화질 시안으로 만들어줘. 예쁘게 말고 구조만. 모바일·PC 둘 다 안 깨지게 최소한의 반응형으로. 다 만들면 파일로 저장하고 열어볼 수 있게 해줘.
> 2. **함정 스코핑**: 이 서비스에 아래 중 뭐가 해당하는지 판단해줘 — ⓐ 관리자가 데이터를 입력·수정·삭제하나 ⓑ 돈(가격·결제·정산)을 다루나 ⓒ 외부 API(결제·문자·로그인·검색 등)에 연동하나 ⓓ 남에게 공유·검색되나(카톡·구글) ⓔ 사용자가 글/폼을 입력하나 ⓕ 날짜·시간을 보여주나.
> 3. **설계 지시서**: 위에서 해당되는 것마다 "코드 짜기 전에 이렇게 설계하라"를 비개발자도 알게 한 줄씩 정리해줘. 예: 어드민 있음 → 저장은 덮어쓰기 전 백업, 삭제는 소프트삭제(deleted_at), 권한은 서버에서 검사 / 돈 있음 → 금액은 원 단위 정수 / 공유됨 → OG태그·robots·sitemap.
>
> 그런 다음 이 설계대로 실제 개발을 진행하면 돼. (다 만든 뒤엔 아래 "🎯 한 방에 다"로 최종 점검.)

---

## 🎯 한 방에 다 (배포 전 종합 세팅)

> 이 프로젝트를 실제 서비스로 배포하려고 해. 아래 "배포 전 필수"를 점검하고, 빠진 건 내 코드에 맞게 구현해줘. 각 항목마다 "이미 됨 / 방금 추가함 / 해당 없음"으로 결과를 알려줘.
>
> **데이터·보안**
> 1. 데이터를 통째로 덮어쓰는 저장이 있으면, 덮어쓰기 직전에 기존 값을 타임스탬프 붙여 한 벌 백업해 (복구 가능하게).
> 2. 삭제 기능이 있으면 진짜 삭제 말고 `deleted_at` 소프트삭제로 바꾸고, 목록 조회에서 걸러줘.
> 3. 관리자·권한 기능이 있으면 권한 검사를 **서버(API)에서** 하게 해줘. 프론트에서 버튼 숨기는 것만으론 안 돼.
> 4. 비밀번호·API키가 코드에 하드코딩됐거나 기본값 폴백이 있으면 환경변수로 빼고, 환경변수가 없으면 통과가 아니라 거부하게 해줘.
>
> **외부 연동·안정성**
> 5. 외부 API(결제·문자·검색 등) 호출에 타임아웃 + 재시도(2~3회) + 실패 처리를 넣어줘. 같은 요청이 두 번 와도 중복 처리 안 되게(멱등성) 해줘.
> 6. 에러가 나면 서버 로그(console.error)에 남겨줘. 돈·중요 데이터를 바꾸는 곳엔 "무엇을 바꿨나" 한 줄 로그도.
> 7. 시간은 UTC로 저장하고 화면에 보여줄 때 한국시간으로 변환해. 지금 코드에 모호한 부분 있으면 고쳐줘.
> 8. 금액 계산에 소수(float)를 쓰는 곳이 있으면 원 단위 정수 계산으로 바꿔줘 (× 0.9 대신 × 90 ÷ 100).
>
> 각각 왜 그렇게 했는지 한 줄씩 설명해줘.

---

## 🖼️ 공유·검색 세팅 (OG / SEO / GEO / 트래킹)

> 이 사이트에 OG·페이지 제목 등 필요한 걸 다 만들고 최적화해줘. SEO/GEO 기본 최적화도 하고, 내가 설치해야 할 트래킹 코드는 스텝바이스텝으로 쉽게 설명해줘. 구체적으로:
>
> 1. **페이지 제목(title)과 meta description**을 실제 내용에 맞게 채워줘 ("Create Next App" 같은 기본값이 남아 있으면 바꿔). `<html lang="ko">`도 확인.
> 2. **OG 태그**(og:title, og:description, og:image, og:url)와 **트위터 카드**를 넣어줘. 카톡·SNS에 공유했을 때 이미지랑 같이 뜨게. og:image는 1200×630 실제 이미지를 쓰고, 없으면 어떤 이미지를 준비해야 하는지 알려줘.
> 3. **favicon**(탭 아이콘) 확인.
> 4. **robots.txt**와 **sitemap.xml**을 만들어줘.
> 5. **canonical URL** 설정.
> 6. **구조화 데이터(JSON-LD, schema.org)**를 이 사이트 성격에 맞는 타입으로 넣어줘 (AI·검색이 내용을 정확히 읽게).
> 7. **구글 애널리틱스(GA4) 설치**: 어디서 측정 ID를 발급받고 어디에 코드를 넣는지 초보도 따라 하게 번호 매겨 설명해줘. 광고를 돌린다면 메타 픽셀도.
>
> 프레임워크(정적 HTML / Next.js 등)에 맞는 방식으로 해줘.

---

## 개별 프롬프트 (필요한 것만)

**백업 안전판**
> 이 프로젝트에서 데이터를 파일/DB에 통째로 덮어쓰는 부분을 찾아서, 덮어쓰기 직전에 기존 값을 날짜 붙인 이름으로 백업하게 고쳐줘. 백업 실패가 본 저장을 막지 않게.

**외부 API 안정화**
> 이 프로젝트의 모든 외부 API 호출(fetch/axios)에 8초 타임아웃과 429/5xx 재시도 3회를 넣어줘. 여러 건을 순차 처리하는 곳은 한 건 실패가 전체를 멈추지 않게 부분 성공으로 바꿔줘.

**시간대 점검**
> 이 프로젝트에서 날짜/시간을 저장·표시하는 부분을 전부 점검해서, UTC로 저장하고 화면엔 한국시간(Asia/Seoul)으로 보여주게 통일해줘. `new Date()`를 시간대 없이 저장하는 곳이 있으면 알려줘.

**금액 계산 점검**
> 이 프로젝트에서 금액(가격·할인·부가세·정산)을 계산하는 부분을 찾아서, 소수(float) 대신 원 단위 정수로 계산하게 바꿔줘. 반올림 규칙도 한 곳으로 통일해줘.

**트래킹 설치 (초보용 단계별)**
> 구글 애널리틱스(GA4)를 이 사이트에 설치하고 싶어. ①계정·속성 만드는 법 ②측정 ID 발급받는 법 ③코드를 어디에 넣는지 ④설치가 됐는지 확인하는 법을, 컴퓨터 잘 모르는 사람도 따라 하게 번호 매겨 알려줘. 그리고 실제 코드도 내 프로젝트에 넣어줘.

---

> 💡 이 프롬프트로 나온 결과가 맞는지 확신이 안 서면, [README](README.md)의 체크리스트로 하나씩 대조해보세요.
